# دليل المستخدم وشرح الأكواد البرمجية - تطبيق Jordan Explorer

هذا الملف يهدف إلى تقديم شرح مبسط وشامل لجميع الأكواد والملفات المستخدمة في بناء تطبيق "Jordan Explorer". تم تصميم هذا الدليل ليساعدك على فهم هيكل المشروع وكيفية عمل كل شاشة فيه، وهو مصمم خصيصاً لتلبية متطلبات مشروع التطوير البرمجي (BTEC Level 3).

---

## 📁 هيكل المشروع (Project Structure)
يتكون المشروع من مجموعة مجلدات رئيسية:
- **`lib/`**: هذا هو المجلد الأهم، ويحتوي على كل الأكواد البرمجية (Dart) التي تشغل التطبيق وتبني واجهاته.
- **`assets/`**: يحتوي على جميع الملفات والميديا المرفقة بالتطبيق مثل:
  - `images/`: صور المعالم السياحية والخلفيات.
  - `video/`: الفيديو التعريفي لمعالم الأردن.
  - `audio/`: المؤثرات الصوتية (لتشغيلها عند التقييم الممتاز والنجاح في الاختبار).
- **`pubspec.yaml`**: ملف إعدادات وعصب المشروع، وفيه نحدد الحزم والمكتبات الخارجية (Packages) التي استخدمناها مثل تشغيل الفيديو (`video_player`) وتشغيل الصوت (`audioplayers`)، بالإضافة إلى تعريف مسارات الصور المرفقة ليتمكن التطبيق من قراءتها.

---

## 📄 شرح ملفات الكود (أكواد مجلد `lib/`)

### 1. الكود الأساسي `main.dart` (نقطة البداية)
هو الملف الذي يبدأ منه تشغيل التطبيق (Entry Point).
- **وظيفته**: إعداد الثيم العام للتطبيق (الألوان، نوع التصميم الأساسي) وتعيين الواجهة الأولى التي ستظهر للمستخدم عند فتح التطبيق.
- **محتواه**: يحتوي على ودجت `MaterialApp` الذي يدير آلية عمل التطبيق كاملاً، ويوجه المستخدم تلقائياً إلى `MainScreen` التي تحتوي على شريط التنقل.

### 2. قالب النظام `screens/main_screen.dart` (شريط التنقل السفلي)
- **وظيفته**: يمثل القالب الأساسي للتطبيق. يحتوي على **شريط تنقل سفلي (Bottom Navigation Bar)** يتيح للمستخدم التنقل بين 4 صفحات رئيسية (الصفحة الرئيسية، المعالم، الاختبار، التقييم) بسرعة وبدون الحاجة للرجوع للخلف.
- **كيف يعمل؟**: يستخدم كود يسمى `StatefulWidget` لأنه يحتاج إلى تحديث وتغيير واجهة العرض المعروضة في منتصف الشاشة بناءً على الأيقونة التي يختارها المستخدم من الشريط السفلي.

### 3. صفحة البداية `screens/home_screen.dart` (الشاشة الرئيسية)
- **وظيفته**: أول نظرة يلقيها المستخدم. تحتوي على الشعار (Logo)، نص ترحيبي وتعريفي بسيط عن الأردن مكتوب بطريقة فنية ومريحة للعين، وزر بارز يسمى "Watch Introductory Video".
- **كيف يعمل؟**: عند الضغط على الزر، يقوم الكود باستخدام أمر `Navigator` لنقل المستخدم بسلاسة إلى شاشة عرض الفيديو المنتظرة (`VideoScreen`).

### 4. كود مشغل الفيديو `screens/video_screen.dart`
- **وظيفته**: تشغيل مقطع فيديو ترويجي يبرز جمال السياحة في أرجاء الأردن.
- **المكتبة المستخدمة**: استخدمنا أداة `video_player` المدمجة والفعالة للغاية.
- **شرح الأكواد**: 
  - في دالة `initState` (وهي الدالة التي تعمل تلقائياً عند فتح الشاشة): يتم إحضار الفيديو من مجلد `assets/video/ved.mp4` داخل التطبيق ويتم تجهيزه للتشغيل.
  - أضفنا كود لـ **زر الشاشة الكاملة (Fullscreen)** ليتمكن المستخدم من تكبير الفيديو بعرض الشاشة لمشاهدة أفضل، بالإضافة إلى زر إيقاف وتشغيل تفاعلي (Play/Pause) مبرمج ليظهر فوق الفيديو.
  - في دالة `dispose` (دالة إغلاق الشاشة): كتبنا كوداً مهماً ليتأكد من تدمير مشغل الفيديو في الخلفية نهائياً لتوفير موارد وبطارية الهاتف.

### 5. قائمة المعالم `screens/attractions_screen.dart`
- **وظيفته**: يعرض قائمة بجميع المعالم السياحية (البتراء، جرش، وادي رم، إلخ) المتوفرة في التطبيق.
- **شرح الأكواد**:
  - تم ترتيب بيانات كل مدينة داخل هيكل بيانات لسهولة استدعائها.
  - الكود يولد أزراراً أنيقة، كل زر يحمل (اسم المدينة + صورتها المصغرة).
  - الميزة الأهم: عند الضغط على أي معلم، يتم تمرير **(مُعرف المعلم - ID)** إلى شاشة المخرجات الثانية (`AttractionDetailsScreen`) لتعرف الشاشة الثانية أي مدينة أختار المستخدم بالتحديد وتعرض معلوماتها.

### 6. شاشة تفاصيل المدينة `screens/attraction_details_screen.dart`
- **وظيفته**: استقبال هوية (ID) المعلم المختار وعرض معلوماته المفصلة وصوره الكبيرة.
- **شرح الكود السحري والمميز فيها**:
  - **معرض الصور المهني (Image Carousel/Slider)**: قمنا بكتابة كود يعتمد على `PageView`. هذا الكود يأخذ الصور ويبني حولها معرض صور تفاعلي رائع يتيح للمستخدم سحب الشاشة (Swipe) يميناً ويساراً للانتقال بين الصورتين بسلاسة. تم برمجة الكود ليحافظ على نسبة طول وعرض الصور الحقيقية دون أي عبث أو شد (Stretching).
  - الكود يرسم **مؤشر نقاط سفلية (Dots)** تتفاعل تلقائياً لتدل المستخدم على رقم الصورة الحالية.

### 7. شاشة الذكاء والمنافسة `screens/quiz_screen.dart` (الاختبار)
- **وظيفته**: يقدم 5 أسئلة متعددة الاختيارات لقياس مدى انتباه المستخدم لرحلته وتاريخ الأردن.
- **كيف يعمل الكود؟**:
  - الكود يعتمد على تتبع رقم السؤال الحالي `_currentQuestionIndex` ومجموع النقاط `_score`. كلما اختار المستخدم إجابة صحيحة، يرتفع العداد.
  - بمجرد الوصول للسؤال الخامس والإجابة عليه، يعرض الكود رسالة منبثقة تحمل النتيجة المئوية.
  - **التفاعل الصوتي (الميزة المُطورة V1.1)**: تم كتابة كود مبرمج لاختبار ذكاء المستخدم، بحيث أنه إذا وفُق بالحصول على علامة عالية (4 أو 5 نقاط)، يستدعي مكتبة `audioplayers` لتشغيل ملف `cheer.mp3` للاحتفال به. الكود مبرمج ليوقف الصوت تلقائياً بعد مرور 3 ثوانٍ بالضبط لعدم إزعاج المستخدم.

### 8. شاشة التقييم والإحصاء `screens/rating_screen.dart`
- **وظيفته**: شاشة تتيح للمستخدم منح تقييم بنظام الـ 5 نجوم (Star Rating) وكتابة مراجعة.
- **كيف يعمل الكود؟**:
  - نظام نجوم حي: كل النجوم عبارة عن أزرار فارغة يتم تحديث حالتها بمجرد لمس أي نجمة لتُضيء باللون الذهبي كل النجوم التي قبلها بناءً على الترتيب البرمجي (Index).
  - **التقدير المعنوي والتفاعل الصوتي (الميزة المُطورة V1.1)**: الكود ذكي كفاية ليتحقق مما إذا كان إعجاب المستخدم بالتطبيق كبيراً (وضع 4 أو 5 نجوم). إذا كان الجواب نعم، وتحديداً لحظة ضغط المستخدم على زر (إرسال)، يتم إرسال أمر لتشغيل المؤثر الصوتي للرضا والتصفيق (`clap.mp3`) كرسالة شكر وتقديراً لدعمه وتفاعله، وأيضاً يتوقف هذا الصوت برمجياً ومباشرًة بعد 3 ثواني.
  - الشاشة تتحول بُناءً على كود الشرطي (if statement) بعد الإرسال الواجهة إلى شاشة "شكر" بديلة تمنع تكرار الإرسال.

---

## 🛠️ مفاهيم برمجية أساسية اعتمد عليها بناء المشروع:
لتوضيح الصورة البرمجية بشكل أكبر، هذه أهم تقنيات ومصطلحات Flutter التي جعلت هذا التطبيق حقيقياً:

1. **`StatelessWidget`**: هذا الكود استخدمناه للشاشات والواجهات الثابتة. مثل زر أو شريط علوي، لا تتغير خصائصه أبداً بمجرد أن يتم رسمه على شاشة الهاتف.
2. **`StatefulWidget`**: على النقيض تماماً، استخدمنا هذا الكود القوي في الشاشات "المتحركة والمتفاعلة". مثال: (تغيّر السؤال في صفحة النص، انتقال النجمة من فارغة لممتلئة عند لمسها في صفحة التقييم، أو حركة الصور المعروضة يميناً ويساراً في المعرض).
3. **`Navigator`**: وهو الوسيط الملاحي أو الجسر البرمجي الذي يسمح بنقل المستخدم من صفحة لأخرى كإدخال وإخراج من الدائرة (Push & Pop).
4. **`setState`**: التعويذة السحرية الخاصة بمطوري Flutter. عند وضع أي أمر تفاعلي داخل هذه الدالة، فهي تقوم بإخبار جوال المستخدم: "لقد حدث تغيير ما هنا، قم بإعادة طلاء ورسم الشاشة ككل لتظهر التحديث الجديد للرائي" وهذا ما يعطي إحساس السلاسة.

---

*تم كتابة هذا التقرير ليُرفق مع حزمة تسليم تطبيق (Jordan Explorer) لتوضيح الجهد التقني والمنطقي المبذول في برمجة الواجهات وهيكلة المشروع بشكل كامل (V 1.1).*
